import { useState } from "react";
import { Send } from "lucide-react";
export default function Chat({ messages, onSend, me }) { const [text, setText] = useState(""); return <div className="panel-content"><div className="chat-list">{messages.length === 0 && <div className="empty"><Send size={25} /><b>Start the conversation</b><span>Messages are shared with everyone in this room.</span></div>}{messages.map((m, index) => <div key={m.id || `${m.senderId}-${m.createdAt}-${index}`} className={`message ${m.senderId === me ? "mine" : ""}`}><div className="msg-head"><b>{m.senderName}</b><small>{new Date(m.createdAt).toLocaleTimeString([], { hour: "2-digit", minute: "2-digit" })}</small></div><p>{m.text}</p></div>)}</div><form className="chat-form" onSubmit={(e) => { e.preventDefault(); onSend(text); setText(""); }}><input value={text} onChange={(e) => setText(e.target.value)} placeholder="Message the room…" /><button type="submit"><Send size={16} /></button></form></div>; }
